import lit.formats

config.name = "FloatFormat test suite"
config.test_format = lit.formats.ShTest("0")
config.suffixes = [".lit"]

if @UNIX@ in {"True", 1, "ON"}:
  config.available_features="linux"

config.substitutions.append(("%ff", "@PROJECT_BINARY_DIR@/ff"))
